# Anthropic 2025 case study

The paper reconstructs a simplified digital thread of the reported 2025 Anthropic AI-orchestrated cyber-espionage campaign. This artifact release encodes that thread as four semantically typed lifecycle artifacts:

- model safety policy,
- agent interaction,
- generated scripts,
- runtime activity.

The expected progression is increasing semantic drift across the three transitions:

- Policy -> Agent
- Agent -> Scripts
- Scripts -> Runtime

The bundled reference outputs reproduce the paper-reported hybrid scores of 0.22, 0.54, and 0.70.
